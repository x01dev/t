services:
  - type: web
    name: llama-api
    env: docker
    plan: free          # stays on free tier
    dockerfilePath: ./dockerfile
    envVars:
      - key: OLLAMA_HOST
        value: 0.0.0.0:11434
